# InfluxDB v2 Configuration
# This file serves as a template. Copy it to .env and customize if needed.
# These variables will be used by docker-compose to initialize the InfluxDB container.

# Initial user, password, and organization for InfluxDB setup
DOCKER_INFLUXDB_INIT_USERNAME=admin
DOCKER_INFLUXDB_INIT_PASSWORD=password1234
DOCKER_INFLUXDB_INIT_ORG=bng-telemetry-org

# The primary bucket where telemetry data will be stored
DOCKER_INFLUXDB_INIT_BUCKET=bng-bucket

# A secure admin token for accessing the InfluxDB API.
# It's recommended to change this to a long, random string for production.
DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=my-super-secret-bng-token